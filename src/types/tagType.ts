export enum TagTypes {
  category = "category",
  product = "product",
  order = "order",
  cart = "cart",
}

export const TagTypeList = [
  TagTypes.category,
  TagTypes.product,
  TagTypes.order,
  TagTypes.cart,
];
